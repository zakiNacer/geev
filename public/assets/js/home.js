document.addEventListener('DOMContentLoaded', run);

function run(){
    /*
    const newsArrowLeft = document.querySelector('.accueil_actualites_arrow_left');
    const newsArrowRight = document.querySelector('.accueil_actualites_arrow_right');
    const newsContainer = document.querySelector('.accueil_actualites_list');

    newsArrowLeft.addEventListener('click', function(){
        newsContainer.prepend(newsContainer.lastElementChild);
    });
    newsArrowRight.addEventListener('click', function(){
        newsContainer.appendChild(newsContainer.children[0]);
    })


    const confianceItemsList = document.querySelector('accueil_confiances_list');
    const confianceItems = document.querySelectorAll('.accueil_confiance_item');
    const roundedBtns = document.querySelectorAll('.rounded_btn');
    let lastActivatedBtnInd = 0;

    for(let ind=0; ind<roundedBtns.length; ind++)
        roundedBtns[ind].addEventListener('click', function(){
            if(!this.classList.contains('rounded_btn_active')){
                roundedBtns[lastActivatedBtnInd].classList.remove('rounded_btn_active');
                this.classList.add('rounded_btn_active');

                confianceItemsList.prepend(confianceItems[ind]);

                lastActivatedBtnInd = ind;
            }
        });
    */
}